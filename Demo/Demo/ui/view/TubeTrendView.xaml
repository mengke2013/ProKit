<UserControl x:Class="Demo.ui.view.TubeTrendView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Demo.ui.view"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Canvas Name="plot" >
            <!--
            <ItemsControl  x:Name="Lines"  ItemsSource="{Binding Path=LineModels}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <ContentControl>
                            
                            <Line X1="{Binding Path=X1}" Y1="{Binding Path=Y1}" X2="{Binding Path=X2}" Y2="{Binding Path=Y2}" Stroke="Black" StrokeThickness="1" DataContext="{Binding}"/>
                        </ContentControl>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <Line X1="10" Y1="60" X2="150" Y2="60" Stroke="Black" StrokeThickness="1"/>
            <Line X1="150" Y1="60" X2="150" Y2="190" Stroke="Black" StrokeThickness="1"/>-->
        </Canvas>
    </Grid>
</UserControl>
